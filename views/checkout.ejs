<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>

  <div class="container mt-5">
    <h2 class="mb-4 text-center">Checkout Details</h2>
    <div class="row">
      <!-- Left Column: Form -->
      <div class="col-md-6">
        <form action="/checkout-add" method="POST" class="p-4 border rounded shadow-sm bg-light">
          <div class="mb-3">
            <label for="customername" class="form-label">Customer Name</label>
            <input type="text" class="form-control" id="customername" name="customername" required />
          </div>

          <div class="mb-3">
            <label for="customerphone" class="form-label">Phone Number</label>
            <input type="number" class="form-control" id="customerphone" name="customerphone" required />
          </div>

          <div class="mb-3">
            <label for="seatnumber" class="form-label">Seat Number</label>
            <input type="number" class="form-control" id="seatnumber" name="seatnumber" required />
          </div>

          <div class="mb-3">
            <label for="amount" class="form-label">Amount (INR)</label>
            <input type="number" class="form-control" id="amount" name="amount" required value="<%= grandTotal %>"
              readonly />
          </div>

          <!-- Include form fields here -->
          <button type="submit" class="btn btn-success w-100 mt-3">Payment done</button>

        </form>
      </div>

      <!-- Right Column: QR Code from Database -->
      <div class="col-md-6 d-flex flex-column align-items-center justify-content-center text-center">
        <h1 class="mb-4">$ <%= grandTotal %>
        </h1>

        <% if (user && user.qrcode) { %>
          <img src="/images/uploads/<%= user.qrcode %>" alt="QR Code" class="img-fluid rounded shadow"
            style="max-width: 300px;" />
          <% } else { %>
            <p class="text-muted">No QR code uploaded</p>
            <% } %>
      </div>

    </div>
  </div>


</body>

</html>